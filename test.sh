#!/bin/bash

# Quick Start Script - для быстрого тестирования
# Проверяет настройки и показывает пример использования

set -e

PROJECT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$PROJECT_DIR"

echo "🔍 Telegram Group Parser - Quick Test"
echo "====================================="

# Проверяем виртуальное окружение
if [ ! -d "venv" ]; then
    echo "📦 Настройка не завершена. Запускаем setup..."
    ./setup.sh
fi

# Активируем окружение
source venv/bin/activate

# Проверяем конфигурацию
echo "⚙️  Проверка конфигурации..."
python main.py --check-config

if [ $? -eq 0 ]; then
    echo ""
    echo "✅ Конфигурация корректна!"
    echo ""
    echo "📋 Доступные команды:"
    echo "  ./run.sh                    # Обработать файл по умолчанию"
    echo "  ./run.sh --input sample.csv # Обработать пример файл" 
    echo "  ./run.sh --verbose          # Подробный вывод"
    echo ""
    echo "📂 Файлы в input/:"
    ls -la input/
    echo ""
    echo "🚀 Готов к работе!"
else
    echo ""
    echo "❌ Настройте .env файл перед использованием"
    echo "📝 Скопируйте .env.example в .env и заполните данные Telegram API"
fi
