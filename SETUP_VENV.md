# 🐍 Настройка виртуального окружения (venv)

Этот проект рекомендует использовать виртуальное окружение Python для изоляции зависимостей.

## Быстрая установка

### 1. Создание виртуального окружения

**Linux/Mac:**
```bash
python3 -m venv venv
```

**Windows:**
```bash
python -m venv venv
```

### 2. Активация виртуального окружения

**Linux/Mac:**
```bash
source venv/bin/activate
```

**Windows (PowerShell):**
```bash
venv\Scripts\Activate.ps1
```

**Windows (CMD):**
```bash
venv\Scripts\activate.bat
```

### 3. Установка зависимостей

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### 4. Проверка установки

```bash
python check_dependencies.py
```

## Проверка работы в venv

После активации виртуального окружения вы увидите префикс `(venv)` в командной строке:

```bash
(venv) user@computer:~/spamgroup_parser$
```

## Деактивация venv

Когда закончите работу, деактивируйте виртуальное окружение:

```bash
deactivate
```

## Проверка зависимостей

Скрипт `check_dependencies.py` автоматически проверяет:
- ✅ Версию Python (требуется 3.7+)
- ✅ Наличие виртуального окружения
- ✅ Установленные зависимости
- ✅ Файлы конфигурации
- ✅ Необходимые директории

Запустите проверку:
```bash
python check_dependencies.py
```

## Устранение проблем

### Проблема: "command not found: python3"
**Решение:** Используйте `python` вместо `python3`

### Проблема: "No module named 'venv'"
**Решение:** Установите python3-venv:
- Ubuntu/Debian: `sudo apt-get install python3-venv`
- macOS: уже включен в Python 3

### Проблема: "pip: command not found"
**Решение:** Установите pip:
```bash
python -m ensurepip --upgrade
```

## Автоматическая проверка

При запуске `main.py` автоматически проверяются критические зависимости. Если они отсутствуют, программа выведет сообщение с инструкциями по установке.

