# 🚀 Telegram Group Parser

Мощный и эффективный парсер для извлечения детальной информации о Telegram группах и каналах с использованием Telethon API.

![Python](https://img.shields.io/badge/python-3.7+-blue.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)
![Status](https://img.shields.io/badge/status-active-brightgreen.svg)

## ✨ Основные возможности

### 📊 Детальная информация о группах
- **Базовая информация**: название, username, ID, тип чата
- **Статистика участников**: количество участников, онлайн пользователи
- **Настройки группы**: медленный режим, закрепленные сообщения
- **Связи**: связанные чаты, административная информация
- **Временные метки**: дата создания, последняя проверка

### 🔄 Интеллектуальная обработка
- **Проверка дубликатов**: автоматический пропуск уже обработанных групп
- **Множественные методы извлечения**: fallback методы для получения количества участников
- **Bot API поддержка**: дополнительный метод через Bot API для лучшей точности
- **Обработка ошибок**: умная обработка FloodWait и других ограничений API

### ⚡ Производительность и надежность
- **Настраиваемые задержки**: защита от rate limiting
- **Промежуточные сохранения**: каждые 10 записей
- **Прогресс-бар**: визуальное отслеживание процесса
- **Подробное логирование**: детальные логи для отладки

## 🛠 Установка

### Требования
- Python 3.7+
- Telegram API credentials (API ID и API Hash)
- Активный Telegram аккаунт

### Быстрая установка

```bash
# Клонируем репозиторий
git clone <repository-url>
cd group-parser

# Устанавливаем зависимости
pip install -r requirements.txt

# Настраиваем конфигурацию
cp .env.example .env
# Отредактируйте .env файл с вашими данными
```

### Получение API credentials

1. Перейдите на https://my.telegram.org
2. Войдите в систему с вашим номером телефона
3. Перейдите в "API development tools"
4. Создайте новое приложение
5. Скопируйте `api_id` и `api_hash`

## ⚙️ Конфигурация

Создайте файл `.env` на основе `.env.example`:

```env
# Telegram API settings
API_ID=your_api_id
API_HASH=your_api_hash
PHONE=+1234567890

# Session settings
SESSION_NAME=group_parser_session

# Rate limiting (seconds between requests)
DELAY_BETWEEN_REQUESTS=5
MAX_RETRIES=1

# CSV settings
INPUT_FILE=input/groups.csv
OUTPUT_SUFFIX=_enhanced

# Bot API token (optional, for better member count accuracy)
BOT_TOKEN=your_bot_token
```

## 📝 Формат входных данных

Создайте CSV файл в папке `input/` со следующими колонками:

```csv
id,username,title,date
1234567890,example_group,Example Group,2025-01-01
,another_group,Another Group,2025-01-02
```

**Поддерживаемые форматы идентификаторов:**
- `id`: числовой ID группы
- `username`: username группы (с @ или без)

## 🚀 Использование

### Базовое использование

```bash
# Обработать файл input/groups.csv
python main.py

# С подробным выводом
python main.py --verbose

# Указать конкретный входной файл
python main.py --input custom_groups.csv

# Указать выходной файл
python main.py --input groups.csv --output results.csv
```

### Проверка конфигурации

```bash
# Проверить настройки без запуска парсинга
python main.py --check-config
```

## 📊 Выходные данные

Парсер создает CSV файл с следующими колонками:

| Колонка | Описание |
|---------|----------|
| `id` | ID группы |
| `actual_title` | Актуальное название группы |
| `actual_username` | Актуальный username (@username) |
| `members_count` | Количество участников |
| `chat_type` | Тип чата (group/supergroup/channel) |
| `created_date` | Дата создания группы |
| `check_date` | Дата последней проверки |
| `online_count` | Количество онлайн пользователей |
| `slow_mode_delay` | Задержка медленного режима (секунды) |
| `pinned_message_id` | ID закрепленного сообщения |
| `linked_chat_id` | ID связанного чата |
| `access_status` | Статус доступа (success/access_denied/error) |
| `error_message` | Сообщение об ошибке (если есть) |

## 🔧 Расширенные возможности

### Проверка дубликатов

Парсер автоматически:
- Загружает существующие данные из выходного файла
- Пропускает уже обработанные группы (по ID и username)
- Объединяет новые данные с существующими
- Показывает статистику пропущенных записей

### Множественные методы извлечения участников

1. **participants_count** - прямое получение из атрибута
2. **get_participants** - получение через список участников
3. **full_chat_attr** - получение через полную информацию о чате
4. **bot_api** - fallback через Bot API (если настроен BOT_TOKEN)

### Обработка ошибок

- **FloodWaitError**: автоматическое ожидание + случайная задержка
- **ChannelPrivateError**: корректная обработка приватных каналов
- **UsernameNotOccupiedError**: обработка несуществующих username
- **Повторные попытки**: настраиваемое количество попыток

## 📁 Структура проекта

```
group-parser/
├── main.py                 # Основной скрипт
├── requirements.txt        # Зависимости
├── .env.example           # Пример конфигурации
├── README.md              # Документация
├── src/
│   ├── config.py          # Конфигурация
│   ├── telegram_parser.py # Основная логика парсинга
│   └── logger_config.py   # Настройки логирования
├── input/
│   └── groups.csv         # Входные данные
├── output/                # Результаты парсинга
└── logs/                  # Файлы логов
```

## 📈 Примеры использования

### Пример 1: Базовый анализ групп

```bash
# Подготовьте файл input/groups.csv с группами
python main.py --verbose
```

### Пример 2: Обработка больших списков

```bash
# Для больших списков используйте увеличенные задержки
# Отредактируйте .env: DELAY_BETWEEN_REQUESTS=10
python main.py --input large_groups.csv
```

### Пример 3: Повторная обработка

```bash
# При повторном запуске дубликаты будут пропущены автоматически
python main.py --input updated_groups.csv
```

## 🔍 Мониторинг и отладка

### Логирование

Логи сохраняются в папке `logs/` с именем `telegram_parser_YYYYMMDD.log`

### Уровни логирования

- `INFO`: основная информация о процессе
- `DEBUG`: детальная информация (используйте `--verbose`)
- `WARNING`: предупреждения о проблемах
- `ERROR`: критические ошибки

### Статистика выполнения

После завершения парсер выводит:
- Количество обработанных записей
- Количество успешных операций
- Количество пропущенных дубликатов
- Количество ошибок доступа
- Общее время выполнения

## ⚠️ Ограничения и рекомендации

### Ограничения Telegram API

- **Rate Limiting**: ~30 запросов в секунду
- **FloodWait**: временные блокировки при превышении лимитов
- **Приватные группы**: ограниченный доступ к информации
- **Права доступа**: некоторая информация требует административных прав

### Рекомендации

1. **Используйте разумные задержки** (5+ секунд между запросами)
2. **Обрабатывайте группы порциями** (не более 100-200 за раз)
3. **Регулярно проверяйте логи** на наличие ошибок
4. **Делайте резервные копии** результатов
5. **Соблюдайте ToS Telegram** во избежание блокировки аккаунта

## 🤝 Вклад в проект

Мы приветствуем вклад в развитие проекта! 

### Как внести вклад

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

### Области для улучшения

- [ ] Поддержка дополнительных форматов данных (JSON, XML)
- [ ] Веб-интерфейс для управления парсингом
- [ ] Интеграция с базами данных
- [ ] Расширенная аналитика групп
- [ ] Поддержка прокси для обхода ограничений

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл [LICENSE](LICENSE) для подробностей.

## 🙏 Благодарности

- [Telethon](https://github.com/LonamiWebs/Telethon) - отличная библиотека для работы с Telegram API
- [edogab33/telegram-groups-crawler](https://github.com/edogab33/telegram-groups-crawler) - вдохновение для некоторых функций
- Сообщество разработчиков Telegram за документацию и поддержку

## 📞 Поддержка

Если у вас есть вопросы или проблемы:

1. Проверьте [Issues](../../issues) на наличие похожих проблем
2. Создайте новый Issue с детальным описанием
3. Приложите логи и конфигурацию (без sensitive данных)

---

**⚡ Сделано с ❤️ для сообщества разработчиков**
