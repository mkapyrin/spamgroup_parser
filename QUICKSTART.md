# 🚀 Быстрый старт

## 1. Настройка (1 минута)
```bash
cd /claude-dev/group-parser
./setup.sh
```

## 2. Получение API (3 минуты)
- Перейти: https://my.telegram.org
- Войти с номером телефона
- Создать приложение в "API development tools"
- Скопировать API_ID и API_HASH

## 3. Конфигурация
```bash
cp .env.example .env
# Отредактировать .env - добавить API_ID, API_HASH, PHONE
```

## 4. Добавление данных
```bash
# Скопировать ваш CSV файл
./copy_csv.sh ~/Downloads/от_спамботов.csv

# ИЛИ поместить файл в input/groups.csv вручную
```

## 5. Запуск
```bash
./run.sh                    # Базовый запуск
./run.sh --verbose          # С подробным выводом
```

## 📊 Результат
- Файл `output/groups_enhanced.csv` с полной информацией о группах
- Логи в папке `logs/`
- Добавленные колонки: количество участников, тип чата, статус верификации

## 🛠 Дополнительные команды
```bash
./test.sh                           # Проверка настроек
./demo.sh                           # Полная демонстрация
python utils.py analyze -i file.csv # Анализ CSV
make help                           # Все доступные команды
```

## ❓ Проблемы?
- Проверьте логи в `logs/`
- Запустите `./run.sh --verbose` для подробной диагностики
- Используйте `./test.sh` для проверки конфигурации
