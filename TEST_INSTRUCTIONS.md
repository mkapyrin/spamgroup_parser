# 🧪 Инструкции по тестированию

## Тестирование обработки всех файлов из input/

### 1. Настройка виртуального окружения (venv)

```bash
# Создайте venv
python3 -m venv venv

# Активируйте venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt
```

### 2. Проверка окружения

```bash
# Проверьте зависимости
python check_dependencies.py
```

### 3. Тестирование обработки всех файлов

#### Режим 1: Обработка всех файлов из input/ (рекомендуется)
```bash
python main.py --all-files
```

Этот режим:
- ✅ Находит все CSV файлы в директории `input/`
- ✅ Проверяет, какие файлы уже обработаны (по хешу)
- ✅ Пропускает уже обработанные файлы
- ✅ Обрабатывает только новые или измененные файлы
- ✅ Сохраняет все результаты в единый файл `output/groups_enhanced.csv`
- ✅ Отслеживает обработанные файлы в `logs/processed_files.log`

#### Режим 2: Обработка одного файла
```bash
python main.py --input input/groups.csv
```

### 4. Проверка результатов

После обработки проверьте:
- `output/groups_enhanced.csv` - единый файл со всеми группами
- `logs/processed_files.log` - список обработанных файлов с хешами
- `logs/telegram_parser_YYYYMMDD.log` - детальные логи обработки

### 5. Повторный запуск

При повторном запуске с `--all-files`:
- Файлы, которые уже были обработаны (и не изменялись), будут пропущены
- Измененные файлы будут обработаны заново
- Новые файлы будут добавлены в `output/groups_enhanced.csv`

### 6. Очистка логов обработанных файлов

Если нужно переобработать все файлы:
```bash
rm logs/processed_files.log
```

### 7. Проверка статистики

Программа выводит статистику:
- Всего файлов найдено
- Сколько файлов обработано
- Сколько файлов пропущено (уже обработаны)
- Статистика по группам (успешно, ошибки, доступ запрещен)

## Особенности работы

### Отслеживание обработанных файлов

Система использует MD5 хеш файла для отслеживания:
- Файл считается обработанным, если его хеш совпадает с записью в логе
- Если файл изменен (хеш изменился), он будет обработан заново
- Логи хранятся в `logs/processed_files.log` в формате: `абсолютный_путь|хеш`

### Единый выходной файл

Все результаты сохраняются в `output/groups_enhanced.csv`:
- Дубликаты групп (по ID и username) автоматически пропускаются
- Данные из разных входных файлов объединяются
- При повторной обработке существующие записи обновляются

### Проверка дубликатов на двух уровнях

1. **Уровень файлов**: Файлы не обрабатываются повторно, если не изменились
2. **Уровень групп**: Группы с одинаковыми ID или username пропускаются

