#!/bin/bash

# Telegram Group Parser - Setup Script
# Скрипт для первоначальной настройки проекта

set -e  # Останавливаться при ошибках

echo "🚀 Настройка Telegram Group Parser..."
echo "=================================="

# Определяем директорию проекта
PROJECT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$PROJECT_DIR"

# Проверяем Python
echo "🐍 Проверка Python..."
if ! command -v python3 &> /dev/null; then
    echo "❌ Python 3 не найден. Установите Python 3.7+"
    exit 1
fi

PYTHON_VERSION=$(python3 --version | cut -d' ' -f2 | cut -d'.' -f1-2)
echo "✅ Python $PYTHON_VERSION найден"

# Создаем виртуальное окружение
echo "📦 Создание виртуального окружения..."
if [ ! -d "venv" ]; then
    python3 -m venv venv
    echo "✅ Виртуальное окружение создано"
else
    echo "✅ Виртуальное окружение уже существует"
fi

# Активируем виртуальное окружение
echo "🔄 Активация виртуального окружения..."
source venv/bin/activate

# Обновляем pip
echo "⬆️  Обновление pip..."
pip install --upgrade pip

# Устанавливаем зависимости
echo "📚 Установка зависимостей..."
pip install -r requirements.txt

# Создаем .env файл если его нет
if [ ! -f ".env" ]; then
    echo "⚙️  Создание файла конфигурации..."
    cp .env.example .env
    echo "✅ Создан файл .env - не забудьте его настроить!"
else
    echo "✅ Файл .env уже существует"
fi

# Создаем директории если их нет
mkdir -p input output logs

# Делаем скрипты исполняемыми
chmod +x setup.sh
chmod +x run.sh
chmod +x main.py

echo ""
echo "🎉 Настройка завершена!"
echo "======================"
echo ""
echo "📝 Следующие шаги:"
echo "1. Отредактируйте файл .env - добавьте ваши Telegram API данные"
echo "   - Получите API_ID и API_HASH на https://my.telegram.org"
echo "   - Укажите ваш номер телефона"
echo "2. Поместите ваш CSV файл в папку input/"
echo "3. Запустите: ./run.sh"
echo ""
echo "🔧 Полезные команды:"
echo "  ./run.sh                    # Запуск с настройками по умолчанию"
echo "  ./run.sh --input file.csv   # Указать другой входной файл"
echo "  ./run.sh --verbose          # Подробный вывод"
echo "  ./run.sh --check-config     # Проверить настройки"
echo ""
echo "📖 Документация: README.md"
