#!/bin/bash

# Telegram Group Parser - Run Script
# Скрипт для запуска парсера

set -e

# Определяем директорию проекта
PROJECT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$PROJECT_DIR"

# Проверяем виртуальное окружение
if [ ! -d "venv" ]; then
    echo "❌ Виртуальное окружение не найдено. Запустите ./setup.sh"
    exit 1
fi

# Активируем виртуальное окружение
echo "🔄 Активация виртуального окружения..."
source venv/bin/activate

# Проверяем .env файл
if [ ! -f ".env" ]; then
    echo "❌ Файл .env не найден. Скопируйте .env.example в .env и настройте его"
    exit 1
fi

# Запускаем основной скрипт с переданными аргументами
echo "🚀 Запуск Telegram Group Parser..."
python main.py "$@"
